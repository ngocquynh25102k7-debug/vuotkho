// Firebase config placeholder.
// Replace the placeholder values with your Firebase project's config.
// After filling the values, uncomment the init call to enable remote storage.

const FIREBASE_CONFIG = {
  apiKey: "REPLACE_ME",
  authDomain: "REPLACE_ME",
  projectId: "REPLACE_ME",
  storageBucket: "REPLACE_ME",
  messagingSenderId: "REPLACE_ME",
  appId: "REPLACE_ME",
};

// Uncomment to initialize automatically (after filling values):
// if (window.initFirebase) window.initFirebase(FIREBASE_CONFIG);

// Or call window.initFirebase(FIREBASE_CONFIG) somewhere else after page load.
